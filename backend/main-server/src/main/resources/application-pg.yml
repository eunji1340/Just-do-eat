spring:
  config:
    activate:
      on-profile: pg

  datasource:
    url: jdbc:postgresql://localhost:5432/JDE
    username: root
    password: justdoeat
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379